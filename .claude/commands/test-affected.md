---
description: Run tests + lint only on projects affected by current changes
---

Run `nx affected -t lint test --parallel` and report any failures.
If failures exist, propose fixes file-by-file before editing.
